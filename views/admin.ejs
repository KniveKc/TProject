<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=.0, user-scalable=no">
    <meta(charset='utf-8' )>


        <title>Admin</title>

        <!--   Core JS Files   -->
        <script src="public/nowui/assets/js/core/jquery.3.2.1.min.js" type="text/javascript"></script>
        <script src="public/nowui/assets/js/core/popper.min.js" type="text/javascript"></script>
        <script src="public/nowui/assets/js/core/bootstrap.min.js" type="text/javascript"></script>
        <!--  Plugin for Switches, full documentation here: http://www.jque.re/plugins/version3/bootstrap.switch/ -->
        <script src="public/nowui/assets/js/plugins/bootstrap-switch.js"></script>
        <!--  Plugin for the Sliders, full documentation here: http://refreshless.com/nouislider/ -->
        <script src="public/nowui/assets/js/plugins/nouislider.min.js" type="text/javascript"></script>
        <!--  Plugin for the DatePicker, full documentation here: https://github.com/uxsolutions/bootstrap-datepicker -->
        <script src="public/nowui/assets/js/plugins/bootstrap-datepicker.js" type="text/javascript"></script>
        <!-- Control Center for Now Ui Kit: parallax effects, scripts for the example pages etc -->
        <script src="public/nowui/assets/js/now-ui-kit.js?v=1.1.0" type="text/javascript"></script>


        <script src="https://bootflat.github.io/bootflat/js/icheck.min.js"></script>
        <script src="https://bootflat.github.io/bootflat/js/jquery.fs.selecter.min.js"></script>
        <script src="https://bootflat.github.io/bootflat/js/jquery.fs.stepper.min.js"></script>


        <!--     Fonts and icons     -->
        <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,200" rel="stylesheet" />
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css" />
        <!-- CSS Files -->
        <link href="public/nowui/assets/css/bootstrap.min.css" rel="stylesheet" />
        <link href="public/nowui/assets/css/now-ui-kit.css?v=1.1.0" rel="stylesheet" />
        <!-- CSS Just for demo purpose, don't include it in your project -->
        <link href="public/nowui/assets/css/demo.css" rel="stylesheet" />


        <script src="https://cdn.socket.io/socket.io-1.4.3.js"></script>


        <script src="https://cdn.socket.io/socket.io-1.4.3.js"></script>

        <script type="text/javascript" src="/public/js/paper.js"></script>
        <!-- Load external PaperScript and associate it with myCanvas -->
        <script type="text/paperscript" src="/public/js/myScript.js" canvas="myCanvas"></script>

        <link href="public/css/bootstrap-table.css" rel="stylesheet">        
        <script src="/public/js/bootstrap-table.js"></script>


        <style type="text/css">
            body {
                background: linear-gradient(#000, #333);
            }
            
            .card {
                background-color: rgba(255, 255, 255, 0.1)!important;
            }
            .well {
                background-color: #ddd
            }
            
            .btn-info {
                color: #000;
                background-color: #999;
                border-color: #000000;
                border-radius: 20px;
            }
            
            .btn-info:focus,
            .btn-info.focus {
                color: #fff;
                background-color: #222222;
                border-color: #000000;
            }
            
            .btn-info:hover {
                color: #fff;
                background-color: #444444;
                border-color: #000000;
            }
            
            .btn-info:active,
            .btn-info.active,
            .open>.dropdown-toggle.btn-info {
                color: #fff;
                background-color: #222222;
                border-color: #000000;
            }
            
            .navbar-inverse {
                background-color: #000;
                border: 1px solid #555;
            }

            .navbar .navbar-brand {
                font-size: 1.1em;
            }

            .navbar .navbar-nav p{
                font-size: 0.9em;
            }

            a.nav-link {
                font-size: 0.9em !important;
            }

            .adminDbContainer {
                padding-top: 6em;
            }

            .card .bootstrap-table {
                padding: 10px;
            }

            .card .btn-group {
                padding-bottom: 10px;
            }

            .btn-group>button:not(:last-child){
                margin-right: 5px;
            }

            table td {
                padding-top: 2px !important;
                padding-bottom: 2px !important;
            }
            

        </style>

        <script>
            var socket = io.connect();

            $(document).ready(function() {


                var $table = $('#table');
                var $table2 = $('#table2');

                socket.on("tdata", function(tdata) {;
                    $(function() {

                        $table.bootstrapTable('append', tdata);
                        $table.bootstrapTable('scrollTo', 'bottom');

                    });
                });

                socket.on("tdata2", function(tdata2) {;
                    $(function() {

                        $table2.bootstrapTable('append', tdata2);
                        $table2.bootstrapTable('scrollTo', 'bottom');

                    });
                });

                //#########################################################################

                $(function() {

                    $table.on('click-row.bs.table', function(e, row, $element) {
                        $('.success').removeClass('success');
                        $($element).addClass('success');
                    });
                    $('#button').click(function() {
                        console.log('Selected ID: ' + getSelectedRow().id + ' Selected name: ' + getSelectedRow().name);
                        var dat = {
                            id: getSelectedRow().id,
                            name: getSelectedRow().name,
                            lastname: getSelectedRow().lastname,
                            username: getSelectedRow().username,
                            pwd: getSelectedRow().pwd,
                            ts: getSelectedRow().ts
                        }
                        socket.emit('testdb', dat);
                    });

                });

                function getSelectedRow() {
                    var index = $table.find('tr.success').data('index');
                    return $table.bootstrapTable('getData')[index];
                }

                //##

                $(function() {
                    $table2.on('click-row.bs.table', function(e, row, $element) {
                        $('.success').removeClass('success');
                        $($element).addClass('success');
                    });
                    $('#button2').click(function() {
                        console.log('Selected ID: ' + getSelectedRow2().id + ' Selected name: ' + getSelectedRow2().name);
                        var dat2 = {
                            id: getSelectedRow2().id,
                            name: getSelectedRow2().name,
                            lastname: getSelectedRow2().lastname,
                            username: getSelectedRow2().username,
                            pwd: getSelectedRow2().pwd,
                            ts: getSelectedRow2().ts
                        }
                        socket.emit('testdb2', dat2);

                    });
                });

                function getSelectedRow2() {
                    var index = $table2.find('tr.success').data('index');
                    return $table2.bootstrapTable('getData')[index];
                }

                $('#buttonu').click(function() {

                    socket.emit('deleteuser');

                });

                $('#buttonp').click(function() {

                    socket.emit('deletepending');

                });

                $('#buttonac').click(function() {

                    socket.emit('acceptpending');

                });

                //#########################################################################

            });

            function nameFormatter(value) {
                var str = '';
                for (i = 0; i < value.length; i++) {
                    str = str + '*';
                }
                return str;
            }
        </script>

</head>

<body class="sidebar-collapse">
    <!--background="/public/images/bgimage.jpg"-->

    <!-- NEW NAVBAR -->
    <nav class="navbar navbar-expand-lg bg-primary fixed-top navbar-transparent " color-on-scroll="400">

        <div class="container">
            <div class="navbar-translate">
                <a class="navbar-brand" href="#">Laser Server</a>

                <% if (user) { %>
                <button class="navbar-toggler navbar-toggler" type="button" data-toggle="collapse" data-target="#navigationbarTop" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-bar bar1"></span>
                    <span class="navbar-toggler-bar bar2"></span>
                    <span class="navbar-toggler-bar bar3"></span>
                </button>
                <% } %>
            </div>

            <% if (user) { %>
                <div class="collapse navbar-collapse justify-content-end" id="navigationbarTop" data-nav-image="../assets/img/blurred-image-1.jpg">
                    <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item"><a class="nav-link" data-placement="bottom" target=""><i class="fa fa-user-circle-o"></i><p class="d-lg d-xl"><%= user.username %></p></a></li>
                        <li class="nav-item"><a class="nav-link" href="/gtools">Tools</a></li>

                        <% if (user.username == 'admin') { %>
                        <li class="nav-item"><a class="nav-link" href="/admin">Admin_DB</a></li>
                        <% } %>
                    <% } %>
                    </ul>
                    <% if (user) { %>
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
                    </ul>
                    
                    <% } %>
                </div>
            <% } %>
        </div>
    </nav>
    <!-- NEW NAVBAR END -->

    <!--background="/public/images/bgimage.jpg"-->
    <div class="page-header" filter-color="orange">

        <div class="container adminDbContainer">

            <!-- 1. Tabelle-->
            <div class="card">
                <table data-classes="table table-hover table-condensed tdPadding" data-striped="true" id="table" data-toggle="table" data-search="true" data-height="300">
                    <thead>
                        <tr>
                            <th data-field="id">ID</th>
                            <th data-field="name">Name</th>
                            <th data-field="lastname">Lastame</th>
                            <th data-field="username">Username</th>
                            <th data-field="pwd" data-formatter="nameFormatter">Passwort</th>
                            <th data-field="ts">Timestamp</th>
                        </tr>
                    </thead>
                </table>

                <div class="btn-group">
                    <button id="button" class="btn btn-primary btn-round">Auswahl</button>
                    <button id="buttonu" class="btn btn-primary btn-round">Delete</button>
                    <button class="btn btn-primary btn-round" data-toggle="modal" data-target="#addUserModal">Add User</button>
                </div>
            </div>

            <!-- 2. Tabelle -->

            <div>
                <div class="card">
                    <table data-classes="table table-hover table-condensed" data-striped="true" id="table2" data-toggle="table" data-search="true" data-height="300">
                        <thead>
                            <tr>
                                <th data-field="id">ID</th>
                                <th data-field="name">Name</th>
                                <th data-field="lastname">Lastame</th>
                                <th data-field="username">Username</th>
                                <th data-field="pwd" data-formatter="nameFormatter">Passwort</th>
                                <th data-field="ts">Timestamp</th>
                            </tr>
                        </thead>
                    </table>

                    <div class="btn-group">
                        <button id="button2" class="btn btn-primary btn-round">Auswahl</button>
                        <button id="buttonp" class="btn btn-primary btn-round">Delete</button>
                        <button id="buttonac" class="btn btn-primary btn-round">Accept</button>
                    </div>
                </div>
            </div>

            <!-- Add user Modal öffnen-->

            <div class="modal fade" id="addUserModal" role="dialog">
                <div class="modal-dialog">

                    <!-- Modal content-->
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal">&times;</button>
                            <h4 class="modal-title">Login</h4>
                        </div>
                        <div class="modal-body">
                            <form method="POST" , action="/newUser">
                                <input type="text" name="user[Name]" placeholder="Name">
                                <input type="text" name="user[Lastname]" placeholder="Lastname">
                                <input type="text" name="user[Username]" placeholder="Username">
                                <input type="password" name="user[Password]" placeholder="Password">
                                <input type="submit" value="Submit">
                            </form>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-primary" data-dismiss="modal">Close</button>
                        </div>
                    </div>

                </div>

            </div>
        </div>
    </div>

</body>

</html>